spring:
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Seoul
        format_sql: true
        show_sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/JPA_study?characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull
    username: root
    password: root

#jwt
jwt:
  secret: secrtetsecrtetsecrtetsecrtetsecrtetsecrtetsecrtetsecrtetsecrtet
  issuer: connector